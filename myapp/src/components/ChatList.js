const ChatList = () => {

	return (
		<>
			ChatList
		</>
	)
}

export default ChatList;

// {messages.map((message, index) => (
// 	<div key={index}>
// 		<p>{message.text}</p>
// 		<sup>{message.author}</sup>
// 	</div>
//
// ))}